print("\nЛут в рейде")

N = int(input("Сколько предметов можно унести? : "))
print("Введите ценности предметов (список целых чисел, разделённых пробелами) : ")
values = list(map(int, input().split()))

values.sort(reverse=True)

total = 0
for i in range(N):
    if i < len(values):
        total += values[i]

print(f"Максимальная суммарная ценность: {total}")
